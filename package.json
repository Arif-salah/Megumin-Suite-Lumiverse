{
  "name": "megumin-spindle",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node tools/embed-styles.mjs",
    "build": "npm run prebuild && esbuild src/backend.js --bundle --format=esm --platform=neutral --outfile=dist/backend.js && esbuild src/frontend.js --bundle --format=esm --platform=browser --outfile=dist/frontend.js",
    "check": "node tools/check-imports.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^7.3.1",
    "@fortawesome/free-solid-svg-icons": "^7.3.1",
    "jquery": "^3.7.1"
  }
}
